/*2. Calculate the area of a circle.*/


#include <stdio.h>

  int main () {

    float r = 9; // radius of the circle; r = radius;
    float pi = 3.14; // value of pi;
    
    printf("The area of the circle with radius of %f is %f",r, pi*r*r);
    
      return 0;
}